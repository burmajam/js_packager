<%= "#{Rails.application.class.to_s.split(':').first}.Views.#{name.camelize}_#{@view_name.titleize}" %> = {
	init: function(){
      $("form :input:visible:enabled:first").focus().select();
	  alert("I\'m in RAILS_ROOT/app/javascripts/views/<%= name.underscore %>/<%= @view_name %>.js");
      $("h1").fadeOut('slow').delay(800).fadeIn('slow');
	}
};